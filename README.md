# TODO

[x] 切换预设layer style (build.rs)
[x] 在update layer style后, 解析每一个symbol, 把style更新到symbol中
[x] 增加`update_style(xml_data: String)`, 未来update_style直接操作sch
[x] `update_symbol_content` 在sch中替换instance
[ ] symbol库, 多器件映射到同一个symbol
